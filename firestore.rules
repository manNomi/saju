rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client direct access is blocked. All reads/writes go through server APIs.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
